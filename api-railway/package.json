{
  "name": "stark-agent-api",
  "version": "2.0.0",
  "description": "STARK CFO Virtual - API Railway com autonomia total",
  "main": "server.js",
  "scripts": {
    "start": "prisma db push && node server.js",
    "dev": "prisma db push && node server.js",
    "postinstall": "prisma generate"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.71.2",
    "@prisma/client": "^5.7.0",
    "cors": "^2.8.5",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "prisma": "^5.7.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
